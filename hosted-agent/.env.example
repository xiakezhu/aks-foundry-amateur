# OpenAI-compatible inference used by the hosted Pi agent.
# Passed into the Foundry hosted container as environment_variables.
OPENAI_BASE_URL=https://api.example.com/v1
OPENAI_API_KEY=replace-me
OPENAI_MODEL=your-model
